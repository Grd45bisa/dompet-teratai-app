# On-device ML models

Place exported TFLite models in this folder.

Current planned model:
- `total_line_model.tflite` (from `ml/train_total_line_tf.py`)

This repo does not commit the binary model by default.
